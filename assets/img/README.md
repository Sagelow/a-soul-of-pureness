# Custom artwork

Place the final hand-picked shrine illustration in this folder as:

`background-shrine.jpg`

The page will use it automatically. Until that file is added, the bundled illustrated shrine remains as a visual fallback.
